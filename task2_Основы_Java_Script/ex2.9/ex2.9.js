'use strict';

// Операторы сравнения

// Каким будет результат этих выражений?
5 > 4 // вернёт true
"ананас" > "яблоко" // вернёт false
"2" > "12" // вернёт true
undefined == null // вернёт true
undefined === null // вернёт false
null == "\n0\n" // вернёт false
null === +"\n0\n" // вернёт false